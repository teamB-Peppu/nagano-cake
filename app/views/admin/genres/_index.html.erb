<table>
    <thead>
        <tr>
            <th>ジャンル</th>
            <th colspan="3"></th>
        </tr>
    </thead>
    <tbody>
        <% genres.each do|genre|%>
        <tr>
            <td><%= genre.name %></td>
             <td><%= link_to "編集する", edit_admin_genre_path(genre.id),class: "btn btn-outline-secondary btn-block"%> </td>
        </tr>
        <% end %> 
    </tbody>
    
</table>