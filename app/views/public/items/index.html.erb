<h2>商品一覧（全<%= @items.count %>件）</h2>
<div>
  <% @items.each do |item| %>
    <%= link_to item_path(item.id) do %>
      image
    <% end %>
    <%= item.name %>
    ¥<%= (item.price * 1.1).floor %>
  <% end %>
</div>